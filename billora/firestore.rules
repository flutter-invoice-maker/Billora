rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow authenticated users to access all collections
    // This is for testing - REMOVE IN PRODUCTION
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 